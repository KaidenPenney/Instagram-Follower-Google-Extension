This Google extension allows for an Instagram profile to view all the users who don't follow back. It creates an array of followers and an array of following,
maps the two together to a new array, and returns it in a pop-up window, providing all usernames. It's all done in JavaScript, except for the obvious HTML and css
and uses an autoscroll to collect all the usernames.
